[package]
name = "view-port"
version = "0.1.0"
edition = "2021"

[dependencies]
geometry = { path = "../geometry" }
rendering = { path = "../rendering" }
interactivity = { path = "../interactivity" }
getter-methods = "1.1.0"
