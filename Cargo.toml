[package]
name = "graphics-rs"
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    # layer 0
    "lib/geometry",
    # layer 1
    "lib/rendering",
    # layer 2
    "lib/interactivity",
    # layer 3
    "lib/view-port",
    # layer 4
    "web",

]

[dependencies]
view-port = { path = "lib/view-port" }
