[package]
name = "graphics-rs"
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    # layer 0
    "lib/algebra",
    # layer 1
    "lib/geometry",
    # layer 2
    "lib/rendering",
    # layer 3
    "lib/core",

    "whiteboard", "lib/standard-plugin",
]

[dependencies]
