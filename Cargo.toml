[package]
name = "graphics-rs"
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    # layer 0
    "geometry",
    # layer 1
    "view-port",
    # layer 2
    "interactivity",
    # layer 3
    "web",

    "rendering",
]

[dependencies]
interactivity = { path = "interactivity" }
